二、核心实体类（最重要）
1️⃣ Record 类（核心实体）
这是整个系统最重要的类。
代表：
一条账目记录
类职责
保存：
一条收支数据
例如：
收入：5000
支出：1200
属性设计
class Record {

    private int id;          // 编号
    private String date;     // 日期
    private String type;     // 收入/支出
    private double amount;   // 金额
    private String category; // 类别
    private String note;     // 备注

}
必须实现的方法
构造方法（无参）

构造方法（全参）

getter/setter

toString()
你会练到
✔ 类设计
✔ 封装
✔ 构造方法
✔ toString
这是：
最重要的类
必须最先完成。
2️⃣ User 类（登录用户）
用于：
系统登录
属性
class User {

    private String username;
    private String password;

}
方法
getter/setter
构造方法
这里不用复杂。
先写死：
admin / 123456
即可。
三、核心业务类（系统大脑）
3️⃣ RecordManager（核心大脑）
这个类：
非常非常重要。
类职责
负责：
管理所有记录
包括：
新增
删除
查询
统计
属性
private List<Record> records;
使用：
ArrayList
必须实现的方法（核心）
添加记录
void addRecord(Record record)
作用：
往列表中加入一条记录
删除记录
boolean deleteRecord(int id)
作用：
按ID删除
修改记录
boolean updateRecord(int id, Record newRecord)
显示所有记录
void listRecords()
查询功能
List<Record> searchByDate(String date)

List<Record> searchByType(String type)

List<Record> searchByCategory(String category)
统计功能（重点）
double getTotalIncome()

double getTotalExpense()

double getBalance()
这是：
项目核心计算逻辑
四、工具类（企业常见结构）
4️⃣ FileUtil（文件工具）
作用：
数据持久化
否则程序关闭数据就没了。
职责
保存数据到文件
读取文件到内存
方法
static void saveRecords(List<Record> records)

static List<Record> loadRecords()
使用技术
BufferedWriter
BufferedReader
File
必须掌握。
5️⃣ DateUtil（日期工具）
作用：
获取当前日期
避免手写日期。
方法
static String getCurrentDate()
返回：
2026-04-16
使用：
LocalDate
这是现代Java标准。
五、业务类（登录）
6️⃣ LoginService
作用：
验证用户
方法
boolean login(String username,String password)
逻辑：
用户名==admin
密码==123456
返回：
true / false
即可。
六、主程序类（入口）
7️⃣ MainApp（程序入口）
这是：
系统控制中心
所有功能从这里调用。
属性
RecordManager manager;
必须实现的方法
main()
程序入口：
public static void main(String[] args)
showMenu()
显示菜单：
1 添加收入
2 添加支出
3 查看记录
4 查询
5 删除
6 统计
7 保存
0 退出
addRecord()
处理：
用户输入数据
创建Record对象
调用manager.addRecord()
deleteRecord()
处理：
输入ID
调用manager.deleteRecord()
searchRecord()
处理：
输入日期
调用查询方法
statistics()
处理：
显示收入
显示支出
显示余额
七、类之间关系（非常重要）
关系如下：
MainApp
   |
   v
RecordManager
   |
   v
Record

RecordManager
   |
   v
FileUtil

MainApp
   |
   v
LoginService
   |
   v
User
理解：
MainApp 控制整个系统
RecordManager 管理数据
Record 存储数据
FileUtil 负责存储文件
这就是：
企业典型分层思想
八、推荐开发顺序（严格按这个做）
不要乱顺序。
否则容易卡死。


第1步（最先）
写：
Record.java
完成：
属性
构造方法
toString
getter/setter
这是：
整个系统地基

第2步
写：
RecordManager.java
先实现：
addRecord()
listRecords()
先不要写复杂功能。
第3步
写：
MainApp.java
先做：
简单菜单

1 添加
2 查看
0 退出
这是：
系统骨架
第4步
增加：
deleteRecord()
第5步
增加：
查询功能
第6步
增加：
统计功能
第7步（最后）
写：
FileUtil.java
实现：
保存数据
读取数据
这是：
最终完整版